<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lx.iseau.feature.sos.jellyfishreport.JellyfishReportDAO">

	<insert id="insertReport" parameterType="JellyfishReportDTO"
		useGeneratedKeys="true" keyProperty="reportNumber" keyColumn="report_number">
	  INSERT INTO tb_jellyfish_report
	    (location, mobile, details, image_url, admin_approval)
	  VALUES (
	    ST_SetSRID(ST_MakePoint(#{lon}, #{lat}), 4326)::geography,
	    #{mobile},
	    #{details},
	    #{imageUrl},             
	    COALESCE(#{adminApproval}, 'N')
	  )
	  RETURNING report_number
	</insert>
 
</mapper>
