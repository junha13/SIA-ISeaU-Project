<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lx.iseau.feature.auth.AuthDAO">

<!-- 회원가입 -->
<!-- 정보 저장 -->
<insert id="register" parameterMap="lx.iseau.feature.auth.RegisterDTO">
	insert into tb_user (user_name, email, mobile, id, password)
        values (#{userName}, #{email}, #{mobile}, #{id}, #{password})
</insert>
<!-- 아이디 중복확인 -->
<select id="checkId" parameterType="string" resultType="int">
        select count(*) from tb_user where id = #{id}
</select>

<!-- 로그인 -->
<select id="login" parameterMap="lx.iseau.feature.auth.LoginDTO" resultType="Map">
        select user_number, id from tb_user where id = #{id} and password = #{password}
</select>

<!-- 로그아웃 -->
<delete id="logout" parameterType="int">
    DELETE FROM tb_user WHERE user_number = #{userNumber}
</delete>

<!-- 아이디 찾기 -->
<select id="findId" parameterMap="lx.iseau.feature.auth.FindIdDTO" resultType="string">
    select id from tb_user where user_name = #{userName} and mobile = #{mobile}
</select>
