<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lx.iseau.feature.auth.AuthDAO">

<!-- 회원가입 -->
<!-- 정보 저장 -->
<insert id="register" parameterType="lx.iseau.feature.auth.RegisterDTO">
	insert into tb_user (user_name, email, mobile, id, password)
        values (#{userName}, #{email}, #{mobile}, #{id}, #{password})
</insert>
<!-- 아이디 중복확인 -->
<select id="checkId" parameterType="string" resultType="int">
        select count(*) from tb_user where id = #{id}
</select>

<!-- 로그인 -->
<select id="login" parameterType="lx.iseau.feature.auth.LoginDTO" resultType="Map">
        select user_number, id from tb_user where id = #{id} and password = #{password}
</select>

<!-- 회원탈퇴 -->
<delete id="withdraw" parameterType="int">
    DELETE FROM tb_user WHERE user_number = #{userNumber}
</delete>

<!-- 아이디 찾기 -->
<select id="findId" parameterType="lx.iseau.feature.auth.FindIdDTO" resultType="string">
    select id from tb_user where user_name = #{userName} and mobile = #{mobile}
</select>

<!-- 비밀번호 찾기: 아이디+전화번호로 userNumber찾기 -->
<select id="findUserNumber" parameterType="lx.iseau.feature.auth.FindUserNumberDTO" resultType="int">
    select user_number from tb_user where id = #{id} and mobile = #{mobile}
</select>

<!-- 비밀번호 찾기: 비밀번호 재설정 -->
<update id="resetPassword" parameterType="lx.iseau.feature.auth.ResetPasswordDTO" resultType="int">
    update tb_user set password = #{newPassword} where user_number = #{userNumber}
</update>

</mapper>